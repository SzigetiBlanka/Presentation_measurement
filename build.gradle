// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'
}
}

allprojects {
    repositories {
        google()
        jcenter()
        flatDir {
            dirs 'libs'
        }
    }
}

apply plugin: 'com.android.library'


dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar','*.aar'])
    /*implementation(name: 'exoplayer-r1.5.11', ext:'aar')
    implementation(name: 'gvr-exoplayersupport-release', ext:'aar')
    implementation(name: 'gvr-keyboard', ext:'aar')
    implementation(name: 'gvr-keyboardsupport-release', ext:'aar')
    implementation(name: 'gvr-permissionsupport-release', ext:'aar')
    implementation(name: 'gvr', ext:'aar')
    implementation(name: 'unitygvr', ext:'aar')*/
}

android {
    compileSdkVersion 28
    //buildToolsVersion '29.0.2'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 26
        targetSdkVersion 27
        //applicationId 'com.aut.presentationskills'
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86'
        }
        versionCode 1
        versionName '0.1'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress = ['.unity3d', '.ress', '.resource', '.obb', 'build_info', 'vid_bigbuckbunny.mp4']
    }

    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            jniDebuggable true
        }
        release {
            minifyEnabled true

            proguardFiles.add(file('../../proguard-gvr.txt'))

            /*minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.debug*/
        }
    }

    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/arm64-v8a/*.so'
        doNotStrip '*/x86/*.so'
    }
}
